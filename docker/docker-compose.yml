name: xtract

services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - xtract_data:/data
    restart: unless-stopped

volumes:
  xtract_data:
